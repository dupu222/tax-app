# 快捷菜单样式验证指南

## 更改概述

本次更新重构了首页的快捷菜单样式，使其符合设计图规范。

## 主要变更

### 1. 布局变更
- **之前**: 垂直列表布局，每个业务项占据全宽
- **现在**: 2列网格布局，卡片式设计

### 2. 卡片样式
- 白色背景 (`#ffffff`)
- 8px 圆角
- 柔和阴影效果
- 最小高度 160px

### 3. 图标样式
- 尺寸: 48x48 像素
- 圆角: 12px
- 位置: 卡片顶部

### 4. 按钮设计
每个业务类型有对应的渐变色按钮:
- **综合所得年度汇算**: 蓝色渐变 (#5d9cec → #4286f5) + "去申报"
- **收入纳税明细**: 紫色渐变 (#9b8edd → #7e6bc7) + "去查询"
- **纳税记录开具**: 青绿色渐变 (#5bd5b0 → #3ec79d) + "去开具"
- **更多功能**: 蓝色渐变 (#5d9cec → #4286f5) + "去使用"

## 验证步骤

### 启动开发服务器

```bash
# 安装依赖
pnpm install

# 启动开发服务器
pnpm dev
```

### 视觉验证清单

访问首页后，检查以下内容:

#### 布局检查
- [ ] 快捷菜单显示为 2 列网格
- [ ] 卡片之间有适当的间距 (15px)
- [ ] 卡片高度一致

#### 卡片样式检查
- [ ] 卡片有白色背景
- [ ] 卡片有圆角 (8px)
- [ ] 卡片有轻微阴影效果
- [ ] 图标显示为 48x48 像素的圆角方形 (12px 圆角)

#### 按钮检查
- [ ] 每个卡片底部有彩色渐变按钮
- [ ] 按钮文本正确显示:
  - 综合所得年度汇算 → "去申报"
  - 收入纳税明细 → "去查询"
  - 纳税记录开具 → "去开具"
  - 更多功能 → "去使用"
- [ ] 按钮颜色符合设计规范
- [ ] 按钮有圆角 (15px)
- [ ] 按钮文字为白色

#### 交互检查
- [ ] 点击按钮时有视觉反馈 (透明度变化 + 轻微缩放)
- [ ] 点击按钮后正确跳转到对应页面

### 响应式验证

在不同设备/屏幕宽度下测试:
- [ ] 移动设备 (375px)
- [ ] 小屏手机 (320px)
- [ ] 大屏手机 (414px)
- [ ] 平板 (768px)

确保:
- 网格布局保持 2 列
- 卡片内容不溢出
- 间距保持一致

### 性能验证

- [ ] 页面加载流畅，无明显卡顿
- [ ] 按钮动画过渡自然
- [ ] 图标加载正常

## 设计对比

将运行结果与设计图对比:
1. 打开设计图 (`/workspace/assets/*.jpg`)
2. 对比卡片布局、颜色、间距、字体大小
3. 确认所有视觉元素匹配

## 已知限制

- 当前按钮颜色硬编码为 4 个类型 (type0-3)
- 如果业务数量超过 4 个，需要扩展颜色方案
- 按钮文本映射基于标题匹配，如果后端返回不同的标题，需要更新映射逻辑

## 回滚方案

如果需要回滚到之前的版本:

```bash
git revert 1d760db
```

或直接切换回 master 分支:

```bash
git checkout master
```
